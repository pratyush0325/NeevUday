[phases.install]
cmds = ["yarn install --frozen-lockfile"]

[phases.build]
cmds = [
  "yarn workspace @setu/shared build",
  "cd apps/server && npx prisma generate && yarn build"
]

[start]
cmd = "node apps/server/dist/index.js"
