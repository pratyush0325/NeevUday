{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn install && yarn workspace @setu/shared build && cd apps/server && npx prisma generate && npx prisma migrate deploy && yarn build"
  },
  "deploy": {
    "startCommand": "node apps/server/dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
